# MongoDB Atlas Configuration
# Get the connection string from MongoDB Atlas that has sample_analytics data included from the atlas website, I don't think I am supposed to share the connection_string openly
MONGODB_CONNECTION_STRING='mongodb+srv://<username:password>@cluster0.a1tdgkm.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0'
MONGODB_DATABASE=sample_analytics
MONGODB_TIMEOUT_MS=5000
MONGODB_MAX_POOL_SIZE=50

# OpenAI Configuration
# Get the API key from "https://platform.openai.com/api-keys", the key I used is invalid now since I got pass the limit, also, I just could've used a free model but didn't because I had less time, I apologize and if you want I can try again with a free model API like grok ig i dunno
OPENAI_API_KEY='sk-proj-ENTER YOUR OWN'
OPENAI_MODEL=gpt-3.5-turbo
OPENAI_TEMPERATURE=0.1
OPENAI_MAX_TOKENS=2000
OPENAI_TIMEOUT=30

# Agent Configuration
MAX_CONVERSATION_HISTORY=20
MAX_QUERY_RESULTS=50
ENABLE_STREAMING=true
DEBUG_MODE=false

##use this template to make the .env file and ensure to do this step before working on
